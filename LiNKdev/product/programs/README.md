# Product programs

Empty in virgin template. After Principal **Go**, Planner creates:

`LiNKdev/product/programs/<program-id>/PROGRAM.md`

and nested `modules/<module>/phases/<phase>/issues/`.

Bootstrap factory history lives in `LiNKdev/factory/programs/bootstrap/` (not here).
