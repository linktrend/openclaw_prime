# Completed Agent Sessions

Closed session records are retained here for provenance. Use the matching file in `docs/handoffs/` for the full end-of-session evidence. Do not silently rewrite completed records.
