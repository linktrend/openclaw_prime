# Active Agent Sessions

One file per active session. Read potentially overlapping records before work. Edit only your own session file; Orchestrators summarize these records in `docs/current-status.md`.
