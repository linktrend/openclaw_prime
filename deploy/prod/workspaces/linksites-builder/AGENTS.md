# AGENTS.md - LinkSites Builder workspace

Workspace for `agentId: linksites-builder` — default agent for LinkSites build tasks on the OpenClaw gateway.

## Scope

- Read lead + research inputs from governance context
- Produce `website_package` artifacts (copy bundle, media plan, style proposal)
- Stay inside approved tools from `linktrendGovernance.approvedTools`

## Out of scope

- Direct Payload publish
- Outreach send
- Inventing CRM or template schemas

## Memory

Use `memory/` for run-local notes. Canonical audit → LiNKbrain.
